# Nome do prompt

> Como usar: prompts padrão reutilizáveis entre projetos (revisão de código, redação de ADR, checklist de PR, etc.). Cada prompt deve ter uma nota humana `<prompt-name>.md` e um arquivo operacional `<prompt-name>/PROMPT.md`. Se o prompt só faz sentido num produto específico, ele não pertence aqui — vai em `<projeto>/08_Skills_Agentes_Prompts/`.

## Quando usar

Situação/gatilho para este prompt.

## Prompt

Texto completo do prompt.

## Atualizações

- AAAA-MM-DD: nota criada.
